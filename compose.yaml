services:
  oscp-api:
    container_name: oscp-api
    restart: always
    build: .
    ports:
      - "3001:3001"
    volumes:
      - .:/app
      - /app/node_modules
    networks:
      - prod-network

networks:
  prod-network:
    external: true
    name: prod-network
